# RTK IT Hackathon 2025

## 📖 О проекте

Интеллектуальная система управления складом (WMS) с интеграцией роботов, импортом инвентарных данных и прогнозированием спроса на основе **ИИ-модели DeepSeek (через OpenRouter API)**.

Разработано в рамках **RTK IT Hackathon 2025** командой **@serbelko**.

---

## ⚙️ Архитектура

```
frontend/  → Angular 17 + Material UI
backend/   → FastAPI + SQLAlchemy + DI + OpenRouter LLM
database/  → PostgreSQL 15
```

- **Frontend** — интерфейс для операторов склада: карта склада, загрузка CSV, таблицы остатков и прогнозов, управление зонами.
- **Backend** — FastAPI-сервис с асинхронным DI, REST API и микросервисом ИИ-прогнозов.
- **AI Service** — LLM-обёртка вокруг DeepSeek (через OpenRouter), формирующая прогноз спроса, времени до исчерпания запасов и рекомендованных заказов.

---

## 🧩 Основные функции

| Функция | Описание |
|----------|-----------|
| 📊 Панель мониторинга | Карта склада, состояния роботов, статистика по зонам |
| 🤖 Телеметрия роботов | Эмуляция данных с сенсоров и отправка на `/api/robots/data` |
| 📥 Импорт CSV | Импорт остатков и статусов товаров через `/api/inventory/import` |
| 🧠 Прогноз спроса | ИИ-модуль `/api/ai/predict` анализирует историю и выдаёт прогноз |
| ⚡ Реальное время | Обновление данных склада по WebSocket или периодическим запросам |
| 💾 История инвентаря | Хранение всех сканов и автоматический апдейт остатков |

---

## 🧠 Интеграция ИИ

ИИ-модуль использует API OpenRouter и модель **`deepseek/deepseek-chat-v3.1:free`** (или fallback `deepseek/deepseek-chat`).

**Логика:**
1. Сервис собирает из БД список товаров, текущие остатки и историю за 30 дней.  
2. Формирует промпт и отправляет его в модель.  
3. Модель возвращает JSON с прогнозом.  
4. Результат сохраняется в таблицу `ai_predictions` и возвращается на фронтенд.

**Формат запроса:**
```bash
POST /api/ai/predict
{
  "period_days": 7,
  "categories": []
}
```

**Ответ:**
```json
{
  "predictions": [
    {
      "product_id": "A123",
      "category": "drinks",
      "expected_demand": 40,
      "expected_stockout_in_days": 5.0,
      "recommended_order_quantity": 60
    }
  ],
  "confidence": 0.91
}
```

---

## 🖥 Backend

### 🧱 Технологии

| Компонент | Назначение |
|------------|-------------|
| FastAPI | REST API, DI и роутинг |
| SQLAlchemy (async) | ORM и транзакции |
| Dependency Injector | IoC контейнер |
| httpx | Вызовы LLM API |
| PostgreSQL | Основная база данных |
| Docker Compose | Деплой и инфраструктура |
| Uvicorn | ASGI-сервер |

### 📚 Документация
- ▶️ [Backend README](back/BACKEND_README.md)

---

## 💻 Frontend

### ⚙️ Технологический стек

| Компонент | Версия |
|------------|---------|
| Angular | 17.x |
| Angular Material | 17.x |
| RxJS | 7.x |
| TypeScript | 5.x |
| SCSS | — |

### 📚 Документация
- ▶️ [FRONTEND README](front/FRONTEND_README.md)

---

## 🐳 Docker Compose

Для удобства оба сервиса разворачиваются одной командой:

```bash
docker compose up -d --build
```

### Пример `docker-compose.yml`
```yaml
version: "3.9"
services:
  backend:
    build: ./backend
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - db
    networks:
      - warehouse

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
    networks:
      - warehouse

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: warehouse
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - warehouse

networks:
  warehouse:

volumes:
  pgdata:
```

---

## 🔌 API Обзор

| Метод | Путь | Назначение |
|--------|------|------------|
| `POST` | `/api/robots/data` | Приём данных от роботов |
| `POST` | `/api/inventory/import` | Импорт CSV инвентаря |
| `GET`  | `/api/inventory/list` | Получить текущие остатки |
| `POST` | `/api/ai/predict` | Запрос к LLM-прогнозатору |
| `GET`  | `/api/dashboard/stats` | Статистика склада |

Подробнее можно найти на http://localhost:8000/docs
---


## 🧩 Пример последовательности работы

1. Роботы отправляют JSON с данными о товарах → `/api/robots/data`.  
2. Backend валидирует и сохраняет в `inventory_history`.  
3. Пользователь загружает CSV остатков.  
4. Фронтенд вызывает `/api/ai/predict` → DeepSeek анализирует историю → возвращает прогноз.  
5. Результат отображается на панели с уровнями запасов.

---


## 🔐 Безопасность и устойчивость

- 🔄 Транзакции и откаты при ошибках SQL.  
- 🧱 Исключения типизированы (422/409/500).  
- ⏳ Retry-логика при обращении к LLM.  
- 🧠 Кэширование прогнозов по `product_id` и `period_days`.  
- 🧾 Логи через `structlog` и `uvicorn.access`.

---



## 👨‍💻 Команда

| Участник | Роль |
|-----------|------|
| **@serbelko** | Backend, AI Integration |
| **@deusexeclipse** | Backend, AI Integration |
| **@rayn1xx** | Frontend, Design |


