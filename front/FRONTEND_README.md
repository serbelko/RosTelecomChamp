# Frontend — RTK IT Hackathon (Warehouse Management Dashboard)

## 📦 Описание

Фронтенд представляет собой **SPA на Angular 17 + Angular Material**, предназначенное для визуализации состояния склада, управления инвентарём и интеграции с ИИ-сервисом прогнозирования спроса.

Приложение подключается к backend-сервису FastAPI (см. README backend), который предоставляет REST API `/api/robots`, `/api/inventory`, `/api/ai/predict` и другие маршруты.

---

## 🧩 Основные модули

### 1. `DashboardModule`
- Главная страница панели мониторинга.
- Карта склада с интерактивной визуализацией (`WarehouseMapComponent`).
- Информация о роботах и зонах (данные с `/api/robots/data`).
- Метрики склада и быстрые действия.

### 2. `InventoryModule`
- Импорт CSV-файлов через `InventoryImportService` (`/api/inventory/import`).
- Таблица остатков, статусные индикаторы (`OK`, `LOW`, `CRITICAL`).
- Кнопка «Загрузить CSV» реализована через `mat-stroked-button`.

### 3. `AIServicesModule`
- Вкладка «Прогноз» вызывает API `/api/ai/predict`.
- Полученные прогнозы (`expected_demand`, `days_until_stockout`, `recommended_order`) отображаются в таблице.
- Используется `HttpClient` + `AIService` (`src/app/core/services/ai.service.ts`).

### 4. `WarehouseMapComponent`
- Компонент визуализации склада (SVG-сетка).
- Раскрашивает ячейки по уровню заполненности.
- Реализовано обновление данных в реальном времени через WebSocket или периодические HTTP-запросы.

---

## ⚙️ Технологический стек

| Компонент | Версия | Назначение |
|------------|---------|-------------|
| Angular | ^17.x | UI-фреймворк |
| Angular Material | ^17.x | UI-компоненты |
| RxJS | ^7.x | Реактивные потоки данных |
| TypeScript | ^5.x | Типобезопасность |
| SCSS | — | Стилизация интерфейса |
| ESLint + Prettier | — | Статический анализ и форматирование |

---

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения
Создайте файл `.env` или `src/environments/environment.ts` со следующими переменными:

```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
  wsUrl: 'ws://localhost:8000/ws',
};
```

> Если проект работает через Docker Compose, убедитесь, что `backend` доступен по `http://backend:8000/api`.

### 3. Запуск dev-сервера
```bash
npm run start
```

Приложение будет доступно по адресу [http://localhost:4200](http://localhost:4200).

---

## 🧠 Интеграция с ИИ

Фронтенд вызывает API `/api/ai/predict`:

```typescript
// ai.service.ts
this.http.post(`${environment.apiUrl}/ai/predict`, {
  period_days: 7,
  categories: [],
});
```

Ответ:
```json
{
  "predictions": [
    {
      "product_id": "A123",
      "category": "drinks",
      "expected_demand": 40,
      "expected_stockout_in_days": 5.0,
      "recommended_order_quantity": 60
    }
  ],
  "confidence": 0.91
}
```

Данные отображаются в таблице с цветовой подсветкой по уровню риска.


> Для работы в связке с backend используйте `docker-compose.yml`, где фронт доступен на `http://localhost:8080`.

---

## 🗂 Структура проекта

```
src/
├── app/
│   ├── core/
│   │   ├── services/
│   │   │   ├── ai.service.ts
│   │   │   ├── inventory.service.ts
│   │   │   ├── robot.service.ts
│   │   │   └── api.interceptor.ts
│   │   └── models/
│   ├── features/
│   │   ├── dashboard/
│   │   │   └── warehouse-map/
│   │   ├── inventory/
│   │   └── ai-predict/
│   ├── shared/
│   │   ├── components/
│   │   └── pipes/
│   └── app.component.ts
├── assets/
└── environments/
```